# SQL Server (Prisma) — เชื่อมผ่าน TCP + SQL auth
DATABASE_URL="sqlserver://localhost:1433;database=yamalshop;user=yamalshop;password=Yamal888!Dev;encrypt=true;trustServerCertificate=true"

# JWT
JWT_SECRET="change-me-to-a-long-random-secret"
JWT_EXPIRES_IN="7d"

# Server
PORT=3000
CORS_ORIGIN="http://localhost:5173"
